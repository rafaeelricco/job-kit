# worker-search-social

Caller supplies CONTRACT_BROWSE + CONTRACT_SEARCH before this surface delta.

## Deltas

1. Post without a JD URL is still a candidate; `url` = post permalink.
2. X spam hard-filter: junior/GTM/outsourcing noise out. Prefer email/dm channel over pure ATS when printed.
3. English hiring: when CONSTRAINTS `market_currencies` is nonempty and
   `work_model.remote` is true, at least one formulation covers those markets.
