# surface-linkedin-jobs

Obey CONTRACT_SEARCH.

## Deltas

1. Location UI (geo modes: CONTRACT_SEARCH step 4). Profile default location is
   not acceptable. Named mode: set/cycle every named CONSTRAINTS location the UI
   offers (incl. Remote when listed). Multi-select → one pass per formulation;
   merge+dedupe URLs after the cycle. Never invent a location the UI does not offer.
