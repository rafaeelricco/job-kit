# Job scout — gate

Every search and extract key present. Missing → Gaps, halt.

Drop when the posting cannot hire this seeker (first match): `listed` onsite or location-restricted place that matches no named `locations` (and `Anywhere` not listed); named onsite place with no shared work_model flag; remote bound to a country the kit has no authorization for; hire-from only in `exclude_locations`; salary currencies none of which are in `market_currencies`; frontmatter `company` slug (schema-dossier "Filename" rule) equal to a slug in `exclude_companies`. Drop a `jd_date` older than the kit `date_posted` window (`24_hours` 1 day, `week` 7, `month` 31; `all_time` or none true → no bound): the search-time date keep saw the card, this sees the page. Blank is not a drop. Never infer authorization or currency from a company or country name. Hire-from is printed location, `work_auth`, `hiring_route`, `eligibility_evidence`, or a title country tag — never the company's country.

Write `eligibility` once per row, from the same evidence: any hire-from clause above fired → `incompatible`; hire-from printed and it names a country, region, or `anywhere` the kit may be hired from (a `legal_authorization.jurisdictions[]` row with `legally_allowed_to_work: Yes`, a named `locations` entry, `direct_regions`, or `Anywhere`) → `confirmed`; no hire-from printed, or printed but comparable to nothing on file → `unknown`. Never write `confirmed` from the company's country or from `work_model: Remote` alone. The row carries `eligibility_evidence` as extracted; the gate never rewrites it.

An `incompatible` row leaves the persist set as a kit drop, except that an existing dossier for its normalized `url` is rewritten per `job-store/references/schemas/schema-dossier.md` "Re-run rules" so the stored row reads `incompatible`.
