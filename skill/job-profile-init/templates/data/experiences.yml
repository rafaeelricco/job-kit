# TODO: one object per role.
# Keys: company, position, location, date, summary, url.label, url.href
# summary = role bullets; job-application may use them as letter evidence (technical cause + outcomes).
[]
