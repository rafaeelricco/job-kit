# TODO: one object per role.
# Keys: company, position, location, date, summary, url.label, url.href
# summary = role bullets; job-resume-refine picks which print and may reword them from the same claims.
[]
