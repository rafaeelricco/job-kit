# cv/

Place a compiled resume PDF here for applications:

- Prefer a tailored PDF produced for the role
- Fallback for job-application: `en-us-resume.pdf` (must open as PDF)

Never attach a `.tex` source. Build tooling is operator-owned; job-application does not
generate LaTeX.
