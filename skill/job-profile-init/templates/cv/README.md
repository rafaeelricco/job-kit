# cv/

Place a compiled resume PDF here for applications:

- Prefer a tailored PDF produced for the role
- Fallback for job-apply: `en-us-resume.pdf` (must open as PDF)

Never attach a `.tex` source. Build tooling is operator-owned; job-apply does not
generate LaTeX.
