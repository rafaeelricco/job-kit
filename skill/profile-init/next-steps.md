# Next steps (print after emit-tree)

0. Ensure job-kit is installed into Aside (`bash scripts/aside/install.sh` from the kit checkout) before scout/apply.
1. Fill Fact-law values: salary, work auth, routes, positions, keywords, experiences, skills…
2. Edit `data/search_packs.yaml` formulations to match stack.
   After a kit rename, `impl` stems must match job-scout reference basenames
   (`surface-linkedin-jobs`, `surface-open-web`, …) — not legacy `search-*`.
3. Add private evidence + name-map rows under `private/` when clients need domain phrases.
4. Place or build `cv/en-us-resume.pdf` before apply attachments.
5. Run `bash scripts/install.sh` if not already (writes `~/.config/profile-root`).
6. LinkedIn browser session must match `data/profiles.yaml` LinkedIn username.
