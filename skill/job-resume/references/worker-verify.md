# worker-verify

Caller pastes PROFILE_ROOT + ad + PDF_TEXT + CONTRACT_RESUME before this
delta. Never the `.tex`.

1. Open nothing but PDF_TEXT and PROFILE_ROOT `data/`. Never `cv/`, `*.tex`,
   `interview-prep`, or `scout/applications/`.
2. Emit the contract Outcome. Findings only, then stop.
