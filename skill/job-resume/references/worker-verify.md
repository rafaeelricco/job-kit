# worker-verify

Caller pastes PROFILE_ROOT + ### Ad + ### Fit + PDF_TEXT + CONTRACT_RESUME
before this delta. Never the `.tex`, never a flow file.

## Deltas

1. Open nothing but PDF_TEXT and PROFILE_ROOT `data/`. Never `cv/`, `*.tex`,
   `interview-prep`, or `scout/applications/`.
2. Emit CONTRACT_RESUME checker output — exactly one Outcome. Findings only,
   then stop.
