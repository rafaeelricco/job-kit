# worker-verify

Caller pastes PROFILE_ROOT + ad + PDF_TEXT + n_base + CONTRACT_RESUME before this
delta. Never the `.tex`, never a flow file. `n_base` is the base `.tex` Skills
token count for check 10.

1. Open nothing but PDF_TEXT (in this brief) and Fact files under
   PROFILE_ROOT `data/` the contract names. Do not compile. Do not rewrite.
2. Emit the contract Outcome. Findings only, then stop.
